extends layout.pug

block content
    if blog
        div(class="w-100 flex justify-center items-center flex-col gap-2.5")
            img(class="h-[500px] w-[500px] mx-auto rounded-lg bg-blue-50 object-cover" src=`/assets/image/${blog.imageUrl}`)
            h1(class="") #{blog.title}
            h2(class="") #{blog.author}
            p(class="") #{blog.body}
            p(class="") #{blog.categories}
            div(class="flex flex-row gap-2.5")
                a(href=`/blog/edit/${blog._id}` class="bg-blue-50 px-5 py-1.5 font-semibold text-blue-600 hover:bg-blue-100 duration duration-300 rounded-lg") Edit
                a(href=`/blog/delete/${blog._id}` class="bg-red-50 px-5 py-1.5 font-semibold text-red-600 hover:bg-red-100 duration duration-300 rounded-lg") Delete
    else 
        h1.text-center.border.rounded-lg.border-rose-500.p-2 Blog Bulunamadı